using UnityEngine;
using System.Collections;

namespace Moon.Demo {
    public class TouchInput : MonoBehaviour {
        void Update() {
            //Vector3 world_pos = Camera.main.ScreenToWorldPoint(Input.mousePosition);
            //if (Input.GetMouseButton(0)) {
            //    GameManager.Instance.OnTouch(world_pos);
            //}
        }
    }
}
